{
  "name": "hono-todo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
		"dev:dev": "wrangler dev --env dev",
    "start": "wrangler dev",
		"start:dev": "wrangler dev --env dev",
		"start:stg": "wrangler dev --env stg"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "hono": "^4.0.0"
  }
}
